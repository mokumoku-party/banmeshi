syntax = "proto3";

enum IngredientUnit {
    quantity = 1;
    grams = 2;
}

message Ingredient {
    string name = 1;
    double amount = 2;
    IngredientUnit unit = 3;
    int64 register_date = 4;
}
